# 使用 ADB 而非标准 MTP 协议进行 Android 传输

目标是让传输速度尽量接近 USB 物理带宽上限，并且在手机传输过程中黑屏锁屏时仍然表现稳定。标准 MTP 协议本身开销较大，且手机锁屏后其会话可能被系统直接杀掉。因此我们选择用 ADB（push/pull）来驱动传输——它不依赖屏幕状态，速度也明显更接近 USB 带宽上限。

**后果**：手机必须开启"开发者选项 → USB 调试"，并在电脑首次连接时手动授权其 RSA 密钥。这是用一次性的设置成本换取传输速度和稳定性的权衡。
