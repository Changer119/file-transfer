# 续传采用整文件重传，而非分片/字节级续传

ADB push 是把一个文件当作一个整体来传输的，本身不支持从某个字节偏移量续传。要自己实现字节级续传，就得在 ADB 之上再搭一层自定义的分片协议。我们决定：续传只在"跳过已经完整传输完成的文件"这个粒度上生效；任何在中断时还没传完的文件，都会从头重新传输。

**后果**：如果一个体积很大的文件（比如几 GB 的视频）快传完时被中断，代价是要整个重传这一个文件，而不是只补传缺失的尾部。这是为了简单性而接受的权衡；如果实践中发现大文件被中断的情况很频繁，再重新评估。
