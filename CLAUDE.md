## Agent skills

### Issue tracker（问题跟踪）

问题（Issue）统一记录在本仓库的 GitHub Issues 中（Changer119/file-transfer），通过 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Domain docs（领域文档）

单一上下文（single-context）布局：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
