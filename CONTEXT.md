# File Transfer

一个个人工具：通过 USB 数据线把照片和文件从手机传到电脑，要求传输快、支持断点续传。

## 词汇表（Language）

**设备（Device）**：
通过 USB 连接到电脑的一部手机，用它的 ADB 序列号来标识——同一部手机重新插入会被识别为同一个 Device，而不是一台新设备。同一时刻只操作一个 Device。

**传输任务（Transfer Task）**：
用户在手机浏览器里选中文件（可能跨多个文件夹）并触发一键传输后创建的工作单元。选择状态跨文件夹累积，但"全选"/"反选"只作用于当前打开的这一个文件夹，不影响已累积的全部选择集合。一个 Transfer Task 内的文件按顺序逐个传输，一次只传一个。Transfer Task 归属于创建它时所连接的 Device，可以被中断、之后再续传。

**续传（Resume）**：
同一个 Device 重新连接后，自动（无需用户操作）从上次中断的地方继续未完成的 Transfer Task。在文件粒度上，续传意味着跳过已经完整传输成功的文件；如果某个文件在传输过程中被中断，会从头重新传输这个文件，而不是从某个字节偏移量续传。续传状态只在当前 App 进程存活期间有效：如果 App 重启，任何未完成的 Transfer Task 都会被直接丢弃，而不会被恢复。如果在等待续传期间，电脑连上了另一个不同的 Device，会弹窗提示用户确认放弃当前未完成任务后才能操作新设备。
_避免使用_："断点续传"不要理解成网络抖动相关的概念——本项目不涉及任何网络传输层，这个说法只指代 USB 连接中断的场景。

**可浏览目录（Browsable Locations）**：
浏览某个 Device 时，暴露出来的一组固定目录：DCIM、Pictures、Movies、Download、Documents，以及一些常见社交 App 的公开媒体目录（比如微信的 `Pictures/WeiXin`）。不会把整个设备文件系统当作通用文件管理器暴露出来。

**覆盖策略（Overwrite Policy）**：
如果目标位置已经存在同名文件，新传入的文件会直接替换它。不重命名、不跳过、不弹窗询问。

**传输后删除（Post-Transfer Deletion）**：
~~每个文件成功传输到电脑后，会立刻删除手机上的源文件~~——issue #11 撤销了这个行为。真机验证时发现，安卓相机会在 DCIM/Camera 等目录里留下 `.pending-*` 这类隐藏临时文件，一旦传输结果的判定在这类边界情况下不可靠，自动删除就会造成不可恢复的数据丢失。现在传输行为在文件粒度上始终等同于"复制"：无论 pushFile 成功还是失败，都不会调用 deleteFile 删除手机上的源文件。`DeviceClient.deleteFile()` 这个操作本身还保留在接口里，只是不再由"传输成功"这件事自动触发。
